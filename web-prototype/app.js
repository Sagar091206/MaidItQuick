const intro=document.querySelector("#intro");document.querySelector("#skipIntro").addEventListener("click",()=>intro.classList.add("is-skipped"));
const state={service:"Home cleaning",type:"Scheduled",day:"Today",time:"10:00 AM"};
const update=()=>{document.querySelector("#summary-service").textContent=state.service;document.querySelector("#summary-type").textContent=state.type.toUpperCase()+" · "+state.day.toUpperCase()+", "+state.time};
document.querySelectorAll(".service-card").forEach(button=>button.addEventListener("click",()=>{document.querySelector(".service-card.selected").classList.remove("selected");button.classList.add("selected");state.service=button.dataset.service;update()}));
document.querySelectorAll(".booking-type button").forEach(button=>button.addEventListener("click",()=>{document.querySelector(".booking-type .active").classList.remove("active");button.classList.add("active");state.type=button.dataset.type;update()}));
document.querySelectorAll(".date").forEach(button=>button.addEventListener("click",()=>{document.querySelector(".date.active").classList.remove("active");button.classList.add("active");state.day=button.querySelector("small").textContent==="TODAY"?"Today":button.querySelector("small").textContent;update()}));
document.querySelectorAll(".time").forEach(button=>button.addEventListener("click",()=>{document.querySelector(".time.active").classList.remove("active");button.classList.add("active");state.time=button.textContent;update()}));
const dialog=document.querySelector("#confirmation");document.querySelector("#continueButton").addEventListener("click",()=>{document.querySelector("#dialog-title").textContent=state.service;document.querySelector("#dialog-copy").textContent=state.day+" at "+state.time+". Next, we’ll collect your address and show the final price.";dialog.showModal()});document.querySelector(".close").addEventListener("click",()=>dialog.close());document.querySelector("#dialog-continue").addEventListener("click",()=>{document.querySelector("#dialog-copy").textContent="Address collection is the next MVP screen. We will connect this to real bookings, payments, and worker assignment next."});update();
