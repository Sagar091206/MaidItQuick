package com.makeitquick.security.dto;

/**
 * Marker for every response produced by the unified customer auth flow
 * ({@code /api/v1/auth}), so controllers can expose one stable return type.
 */
public interface AuthV1Response {
}
