# API errors deliberately expose only controller-provided, user-safe messages.
# This allows the Flutter app to explain validation and KYC approval failures.
server.error.include-message=always
server.error.include-binding-errors=never
